<!-- Generated by doc-agent. Do not edit manually. -->

# `function_agent.llm_agent`

Source: `src/function_agent/llm_agent.py`

## Summary

LLM function-calling agent that executes Python tools from model tool calls.

## Classes

### `LLMFunctionCallResult`

Result from one LLM function-calling turn.

Methods:

- `called_tool(self) -> bool`
- `format(self) -> str`

### `LLMFunctionCallingAgent`

Agent that delegates tool choice to an LLM function-calling API.

Methods:

- `__init__(self, root: Optional[Path] = None, model: str = 'openai/gpt-4o', completion_fn: Optional[CompletionFunction] = None)`
- `list_files(self) -> List[str]`
- `read_file(self, file_name: str) -> str`
- `tools(self) -> List[Dict[str, Any]]`
- `run(self, user_task: str) -> LLMFunctionCallResult`
- `execute_tool(self, tool_name: str, tool_args: Mapping[str, Any]) -> Any`

## Functions

### `litellm_completion(**kwargs) -> Any`

Call LiteLLM completion, imported lazily so tests do not need the package.

### `first_choice_message(response: Any) -> Any`

Return the first model message from a LiteLLM/OpenAI-style response.

### `parse_tool_arguments(arguments: Any) -> Dict[str, Any]`

Parse structured tool arguments from the model response.

### `get_value(value: Any, name: str) -> Any`

Read a field from either a dictionary or an SDK response object.

### `format_result(result: Any) -> str`

Render a function result for display.
