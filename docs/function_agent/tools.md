<!-- Generated by doc-agent. Do not edit manually. -->

# `function_agent.tools`

Source: `src/function_agent/tools.py`

## Summary

Python functions exposed as structured tools for an interactive agent.

## Classes

### `ToolExecutionError`

Raised when a tool call is invalid or unsafe.

Methods: none.

### `Tool`

Python function plus metadata that describes how an agent should call it.

Methods:

- `metadata(self) -> Dict[str, Any]`

### `ToolContext`

Filesystem context used by the local demo tools.

Methods:

- `safe_root(self) -> Path`

### `ToolCallResult`

Result from one executed tool call.

Methods: none.

### `ToolExecutor`

Validates JSON-like tool calls and invokes Python functions.

Methods:

- `__init__(self, tools: Mapping[str, Tool])`
- `metadata(self) -> list[Dict[str, Any]]`
- `execute(self, action: Mapping[str, Any]) -> ToolCallResult`

## Functions

### `build_tool_registry(context: ToolContext) -> Dict[str, Tool]`

Build the tools available to the interactive function-calling agent.

### `list_directory(context: ToolContext, path: str) -> list[str]`

List directory entries under the safe workspace root.

### `read_text_file(context: ToolContext, file_path: str) -> str`

Read a text file under the safe workspace root.

### `resolve_under_root(context: ToolContext, path: str) -> Path`

Resolve a relative path and reject paths outside the safe root.

### `validate_args(tool: Tool, args: Mapping[str, Any]) -> None`

Validate action args against the small JSON Schema subset used here.
